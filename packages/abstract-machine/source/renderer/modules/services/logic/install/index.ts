// #region module
export const initialSetup = async (
    options?: any,
) => {
    // install and configure
        // minio/minio
        // mongo
        // redis
}


export const installPluridServer = async (
    name: string,
    progress: (
        type: string,
        value: number,
    ) => void,
    options?: any,
) => {
    // download plurid server based on name

    // load the downloaded file as docker image

    // configure and run the docker image based on options
}
// #endregion module
